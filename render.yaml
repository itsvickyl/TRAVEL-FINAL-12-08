services:
  - type: web
    name: lollyd-relay
    runtime: node
    plan: free
    rootDir: relay
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: RELAY_KEY
        generateValue: true
      - key: NODE_ENV
        value: production
